<form method="post" action="{{ path('app_fish_delete', {'id': fish.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fish.id) }}">
    <button class="btn">Delete</button>
</form>
